import products from "../data/products";

const routes = {
    home: '/',
    user: '/user',
    register: '/register',
    login: '/login',
    products: '/products',
    product: '/product/:id',
    cart: '/cart',
    order: '/order',

};

export default routes;