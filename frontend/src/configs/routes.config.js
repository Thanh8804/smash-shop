import products from "../data/products";

const routes = {
    home: '/',
    user: '/user',
    register: '/register',
    login: '/login',
    products: '/products',
    product: '/product/:id',
};

export default routes;